<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.co.movmov.mapper.WishMovieMapper">
	<!-- 
		void insertWishMovie(String UserId, int movieNo);
	 -->
	<insert id="insertWishMovie">
		insert movmov_wish_movies
		(wish_movie_no, wish_movie_created_date, movie_no, user_id)
		values
		(wish_movie_no_seq, sysdate, #{movieNo}, #{userId})
	</insert>
</mapper>